spring.datasource.url=jdbc:oracle:thin:@//${ORACLE_HOST:localhost}:${ORACLE_PORT:1521}/${ORACLE_PDB:FREEPDB1}
spring.datasource.username=system
spring.datasource.password=${ORACLE_PASSWORD:TestPass123}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
# enough connections for the large-scale test (50 threads)
spring.datasource.hikari.maximum-pool-size=50
